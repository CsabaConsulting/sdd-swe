[project]
name = "aegis"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "textual>=0.80.0",
    "openai",
    "tenacity",
    "pydantic",
    "opentelemetry-api",
    "opentelemetry-sdk",
    "opentelemetry-exporter-otlp",
    "aiosqlite",
    "imaplib2",
    "python-dotenv",
]
